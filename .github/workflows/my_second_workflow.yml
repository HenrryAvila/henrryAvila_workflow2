name: My second Workflow

on: workflow_dispatch

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4
      - name: Print files
        run: |
          ls
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.12
      - name: Test Model
        run: echo "Test Model"

  my-second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4
      - name: Print files
        run: |
          ls
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.12
      - name: Test Model
        run: echo "Deploy Model to AWS sagemaker"